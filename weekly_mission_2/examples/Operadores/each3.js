// Ejemplo 3: forEach para imprimir los países en letras mayúsculas
const countries = ['Finland', 'Denmark', 'Sweden', 'Norway', 'Iceland']
console.log("Ejemplo 3: Imprimiendo la lista de países en mayúsculas")
countries.forEach((element) => console.log(element.toUpperCase()))

// Ejemplo 3.1:
const countrie = ['Finland', 'Denmark', 'Sweden', 'Norway', 'Iceland']
console.log("Ejemplo 3.1: Imprimiendo la lista de países en minúsculas")
countrie.forEach((element) => console.log(element.toLowerCase()))

// Ejemplo 3.2:
const names = [
    "ALBERTO",
    "Bruno",
    "CRISTIAN",
    "daniela"
]
names.forEach((element)=>console.log(element.toUpperCase()))